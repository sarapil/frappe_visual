# Frappe Visual — Module Map
# فريب فيجوال — خريطة الوحدات

```mermaid
graph LR
    subgraph Frappe Visual
        Core["Frappe Visual Core"]
        Settings["FV Settings"]
    end
    style Core fill:#6366F1,color:#fff
```

> **Note:** This is a placeholder module map. Update with actual modules from `modules.txt`.
